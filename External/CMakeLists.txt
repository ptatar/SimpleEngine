set(RAPIDJSON_DIR ${CMAKE_CURRENT_SOURCE_DIR}/rapidjson)
set(SHADERC_DIR ${CMAKE_CURRENT_SOURCE_DIR}/shaderc)

set(RAPIDJSON_BUILD_EXAMPLES OFF)
set(RAPIDJSON_BUILD_TESTS OFF)
add_subdirectory(${RAPIDJSON_DIR})
set(RAPIDJSON_INC_DIR "${RAPIDJSON_DIR}/include/" PARENT_SCOPE)

set(SHADERC_SKIP_INSTALL ON)
set(SHADERC_SKIP_TESTS ON)

add_subdirectory(${SHADERC_DIR})
set(SHADERC_INC_DIR "${SHADERC_DIR}/libshaderc/include/" PARENT_SCOPE)


set(SPIRV_CROSS_STATIS ON)
set(SPIRV_CROSS_SHARED OFF)
set(SPIRV_CROSS_CLI OFF)
set(SPIRV_CROSS_DIR ${CMAKE_CURRENT_SOURCE_DIR}/SPIRV-Cross)
add_subdirectory(${SPIRV_CROSS_DIR})
set(SPIRV_CROSS_INC_DIR "${SPIRV_CROSS_DIR}" PARENT_SCOPE)

set(YAS_DIR ${CMAKE_CURRENT_SOURCE_DIR}/yas)
set(YAS_INC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/yas/include" PARENT_SCOPE)
